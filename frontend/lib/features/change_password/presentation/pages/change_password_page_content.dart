import 'package:flutter/material.dart';

class ChangePasswordPageContent extends StatelessWidget {
  const ChangePasswordPageContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}