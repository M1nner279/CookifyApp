import 'package:flutter/material.dart';

class CookifyErrorContent extends StatelessWidget {
  const CookifyErrorContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}